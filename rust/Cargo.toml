[package]
name = "project-link"
version = "0.1.0"
edition = "2018"

[dependencies]
serde = { version = "^1.0", features = ["derive"] }
serde_json = "^1.0"

[dev-dependencies]
quickcheck = "^1.0"
quickcheck_macros = "^1.0"
